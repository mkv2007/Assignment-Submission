# Task Manager (Node.js)

A simple command-line Task Manager built using **only core Node.js modules** (`fs` and `process`).  
It allows you to add, list, update, delete, and mark tasks as done.

---

## 📌 Features

- Add a task  
- List all tasks  
- Remove a task by ID  
- Update a task's title  
- Mark a task as DONE  
- Stores data in a local `tasks.json` file  

---

## 📁 Project Files


